import styled from "styled-components";

export const Container = styled.div`
    width: 100%;
    max-width: 900px;

    margin: 40px 0;
    padding: 10px;

    border-radius: 7px;

    background-color: var(--secondary);
`;
